export interface CardModel {
    id: number;
    img_url: string;
    is_mortgage: boolean;
    price: number;
    count_rooms: number;
    square: number;
    floor: string;
    address: string;
    slug: string;
}